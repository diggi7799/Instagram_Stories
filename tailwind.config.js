/** @type {import('tailwindcss').Config} */
const colors = require('tailwindcss/colors')

module.exports = {
  content: ["Insta.html"],
  theme: {
    extend: {
      colors: {
        fuchisa: colors.fuchisa,
      },
    },
  },
  variants: {},
  plugins: [],
}

